// Placeholder — real widget tests need a Supabase test double before they can
// pump ShopHubApp (it initializes a live Supabase client in main()). Add
// proper tests once a fake/mocked SupabaseClient is wired up for the test
// environment.
void main() {}
